@model Data.Dto.CRUD.PreferredPlayingDate.PreferredPlayingDateFormDto

<div class="dashboard-panel-header">
    <h2 class="dashboard-panel-title">@(Model.Id.HasValue ? "Edit Preferred Date" : "Create Preferred Date")</h2>
</div>

<form class="dashboard-form" asp-controller="PreferredPlayingDates" asp-action="@(Model.Id.HasValue ? "Edit" : "Create")" asp-route-id="@Model.Id" method="post" data-dashboard-form>
    @if (Model.Id.HasValue)
    {
        <input type="hidden" name="Id" value="@Model.Id" />
    }

    <div class="dashboard-form-row">
        <label asp-for="Date"></label>
        <input asp-for="Date" type="datetime-local" />
        <span asp-validation-for="Date" class="text-danger"></span>
    </div>

    <div class="dashboard-form-actions">
        <button class="dashboard-submit" type="submit">Save changes</button>
    </div>
</form>
